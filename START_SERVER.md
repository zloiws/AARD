# Инструкция по запуску сервера AARD

## ⚠️ ВАЖНО: Сервер должен быть запущен перед тестированием!

Ошибка `ConnectError: All connection attempts failed` означает, что сервер не запущен.

## 🚀 Как запустить сервер

### Вариант 1: Через PowerShell (рекомендуется)

1. **Откройте НОВЫЙ терминал PowerShell** (не закрывайте текущий!)

2. **Перейдите в директорию backend:**
   ```powershell
   cd C:\work\AARD\backend
   ```

3. **Запустите сервер:**
   ```powershell
   python main.py
   ```

4. **Дождитесь сообщения:**
   ```
   INFO:     Uvicorn running on http://0.0.0.0:8000
   ```

5. **Оставьте этот терминал открытым!** Сервер должен работать постоянно.

### Вариант 2: Через скрипт

**PowerShell:**
```powershell
.\start_server.ps1
```

**CMD:**
```cmd
start_server.bat
```

## 🧪 Тестирование (в другом терминале)

После того, как сервер запущен, в **другом терминале** выполните:

```powershell
cd C:\work\AARD
python backend/test_api_simple.py
```

## ✅ Проверка, что сервер работает

```powershell
curl http://localhost:8000/health
```

Должен вернуть: `{"status": "healthy"}`

## 🔍 Диагностика проблем

### Если сервер не запускается:

1. **Проверьте .env файл:**
   ```powershell
   cd C:\work\AARD
   python backend/fix_env.py
   ```

2. **Проверьте, что порт 8000 свободен:**
   ```powershell
   netstat -ano | findstr :8000
   ```

3. **Проверьте ошибки в терминале сервера** - там будут показаны все ошибки при запуске

### Если .env не загружается:

Убедитесь, что файл `.env` существует в `C:\work\AARD\.env` (не в `backend/.env`!)

## 📝 Структура терминалов

```
Терминал 1 (Сервер):
  cd C:\work\AARD\backend
  python main.py
  [Оставить запущенным]

Терминал 2 (Тесты):
  cd C:\work\AARD
  python backend/test_api_simple.py
```

## 🛑 Остановка сервера

В терминале с сервером нажмите `CTRL+C`

