# 🔍 Диагностика проблемы выбора модели

## Проблема
Модель "huihui_ai/gemma3-abliterated:4b-q8_0" все еще используется, даже когда выбрана другая модель.

## Добавленное логирование

### 1. В `chat.py`:
- `[CHAT] DEBUG: Explicitly selected model from request: {model}` - когда модель явно выбрана
- `[CHAT] DEBUG: No model provided in request, will auto-select` - когда модель не указана
- `[CHAT] DEBUG: Auto-selected model: {model}` - когда модель выбрана автоматически
- `[CHAT] DEBUG: Available models: {list}` - список доступных моделей
- `[CHAT] DEBUG: Final selection - server_url={url}, model={model}` - финальный выбор

### 2. В `ollama_client.py`:
- `[OllamaClient] DEBUG: actual_model_name={name}, instance.model={model}, model_to_use={model}` - выбор модели
- `[OllamaClient] DEBUG: server_url={url}, is_dynamic_instance={bool}` - информация об инстансе
- `[OllamaClient] DEBUG: Using cached response for model={model}` - использование кэша
- `[OllamaClient] DEBUG: Sending request to {url}/api/chat with model={model}` - отправка запроса
- `[OllamaClient] DEBUG: Payload model field: {model}` - модель в payload
- `[OllamaClient] WARNING: Ollama returned model '{returned}' but we requested '{requested}'` - если Ollama вернул другую модель

## Как диагностировать

1. **Перезапустите сервер** с новым логированием
2. **Откройте консоль браузера** (F12) и посмотрите, что отправляется в запросе:
   ```javascript
   console.log('Submitting chat request:', requestData);
   ```
3. **Проверьте логи сервера** - должны быть видны все DEBUG сообщения
4. **Сделайте запрос** с явно выбранной моделью
5. **Проверьте логи**:
   - Какая модель отправлена из фронтенда?
   - Какая модель получена в `chat.py`?
   - Какая модель передана в `OllamaClient`?
   - Какая модель в payload для Ollama API?
   - Что вернул Ollama API?

## Возможные причины

### 1. Кэш
Если запрос с таким же prompt уже был сделан с моделью gemma3, ответ может браться из кэша.
**Решение**: Кэш учитывает модель в ключе, но проверьте логи - если видите "Using cached response", значит используется кэш.

### 2. Модель не передается из фронтенда
Проверьте, что в `requestData` есть поле `model` с правильным значением.

### 3. Автовыбор модели
Если модель не выбрана явно, выбирается первая модель из списка (строка 161 в chat.py).
**Решение**: Всегда выбирайте модель явно в интерфейсе.

### 4. Ollama возвращает другую модель
Ollama API может вернуть другую модель в ответе (например, если используется алиас).
**Решение**: Мы всегда используем `model_to_use` (запрошенную модель), а не то, что вернул Ollama.

## Следующие шаги

1. Перезапустите сервер
2. Откройте консоль браузера
3. Сделайте запрос с явно выбранной моделью
4. Проверьте логи сервера
5. Пришлите логи для анализа

