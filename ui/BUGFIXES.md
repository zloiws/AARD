# 🐛 Исправления багов

## ✅ Исправлено

### 1. Проблема с task_id в plans.list

**Проблема:**
```
GET /api/plans?task_id=[object%20Object] HTTP/1.1" 422 Unprocessable Entity
```

**Причина:**
Где-то передавался объект вместо строки для `task_id` параметра.

**Решение:**
- ✅ Добавлена строгая проверка типа в `api.plans.list()`
- ✅ Автоматическое извлечение `id` или `task_id` из объекта если передан объект
- ✅ Логирование ошибок для отладки
- ✅ Обновлен `usePlans()` hook для поддержки опционального `taskId` параметра

**Файлы изменены:**
- `lib/api/client.ts` - добавлена защита от объектов
- `lib/hooks/use-api.ts` - обновлен usePlans для поддержки taskId

### 2. Компонент для планов задачи

**Добавлено:**
- ✅ Компонент `TaskPlans` для отображения планов конкретной задачи
- ✅ Интегрирован в страницу деталей задачи
- ✅ Правильная обработка taskId (всегда строка)

**Файлы:**
- `components/tasks/task-plans.tsx` (новый)
- `app/tasks/[id]/page.tsx` (обновлен)

---

## 🔍 Как проверить

1. Откройте страницу деталей задачи: `/tasks/[id]`
2. Внизу страницы должен быть раздел "Plans"
3. Если есть планы для этой задачи - они отобразятся
4. В консоли не должно быть ошибок `[object Object]`

---

## 📝 Технические детали

### Защита от объектов

```typescript
// В api.plans.list()
if (taskId !== undefined && taskId !== null) {
  if (typeof taskId !== 'string') {
    // Попытка извлечь id из объекта
    const id = taskId?.id || taskId?.task_id
    if (id && typeof id === 'string') {
      return fetchApi<Plan[]>(`/api/plans?task_id=${id}`)
    }
    // Возвращаем все планы если не удалось извлечь id
    return fetchApi<Plan[]>('/api/plans')
  }
  // Валидная строка
  return fetchApi<Plan[]>(`/api/plans?task_id=${taskId}`)
}
```

### Использование

```typescript
// Правильно - передаем строку
usePlans('task-id-string')

// Защита - если передан объект, извлечет id
usePlans(task.task_id) // task.task_id может быть объектом
```

---

**Проблема исправлена!** Теперь все запросы к `/api/plans` должны работать корректно. ✅

---

### 3. Проблема с отображением strategy как объекта

**Проблема:**
```
Objects are not valid as a React child (found: object with keys {approach, assumptions, constraints, success_criteria})
```

**Причина:**
`plan.strategy` это JSON объект, а не строка. Backend хранит strategy как JSON с полями:
- `approach`
- `assumptions`
- `constraints`
- `success_criteria`

**Решение:**
- ✅ Обновлен тип `Plan.strategy` в API client
- ✅ Добавлена проверка типа перед отображением
- ✅ Правильное извлечение текста из объекта strategy
- ✅ Детальное отображение всех полей strategy на странице деталей плана

**Файлы изменены:**
- `lib/api/client.ts` - обновлен тип Plan
- `app/plans/page.tsx` - исправлено отображение strategy
- `app/plans/[id]/page.tsx` - детальное отображение strategy объекта
- `app/search/page.tsx` - исправлен поиск по strategy
- `components/tasks/task-plans.tsx` - исправлено отображение strategy

**Результат:**
Теперь strategy правильно отображается:
- Если строка - показывается как есть
- Если объект - извлекается `approach` или `success_criteria` для краткого отображения
- На странице деталей - все поля strategy отображаются отдельно

---

### 4. Проблема с выбором модели для чата

**Проблема:**
```
OllamaError: HTTP error from http://10.39.0.6:11434/v1: 400 - {"error":"\"embeddinggemma:latest\" does not support chat"}
```

**Причина:**
Система выбирала первую доступную модель из базы данных без проверки, поддерживает ли она чат. Модели для embeddings (например, `embeddinggemma:latest`) не поддерживают chat API.

**Решение:**
- ✅ Добавлена фильтрация embedding моделей (по ключевым словам в названии: 'embedding', 'embed')
- ✅ Приоритет моделям с capabilities для чата
- ✅ Улучшена обработка ошибок с понятными сообщениями
- ✅ Логирование выбранных моделей для отладки

**Файлы изменены:**
- `backend/app/api/routes/chat.py` - добавлена фильтрация моделей и улучшена обработка ошибок

**Результат:**
Теперь система:
- Исключает embedding модели при выборе модели для чата
- Предпочитает модели с capabilities для чата
- Показывает понятные сообщения об ошибках, если модель не поддерживает чат
