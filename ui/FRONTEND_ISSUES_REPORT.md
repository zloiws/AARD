# Отчет о проблемах и недоработках фронтенда

## 🔍 Обзор

Проанализирован фронтенд проекта AARD (папка `ui/`). Выявлены заглушки, недоработки и проблемы, особенно в части построения воркфлоу в чате.

---

## 🚨 Критические проблемы

### 1. **Отсутствие интерактивного графа воркфлоу в чате** ✅ ИСПРАВЛЕНО

**Проблема:**
- В чате (`app/chat/page.tsx` и `app/chat/[taskId]/page.tsx`) воркфлоу отображается только через компонент `WorkflowTimeline` в свернутом виде (`<details>`)
- Нет полноценного интерактивного графа воркфлоу прямо в чате
- Пользователь должен открывать отдельную страницу `/workflows/[workflowId]` для просмотра графа

**Решение:**
- ✅ Создан новый компонент `WorkflowView` (`components/chat/workflow-view.tsx`)
- ✅ Добавлено переключение между Timeline и Graph view прямо в чате
- ✅ Интегрирован `EnhancedWorkflowGraph` в сообщения чата
- ✅ Компонент поддерживает компактный режим для чата и полный режим для отдельной страницы

**Файлы:**
- ✅ `ui/components/chat/workflow-view.tsx` (новый компонент)
- ✅ `ui/app/chat/page.tsx` (обновлен)
- ✅ `ui/app/chat/[taskId]/page.tsx` (обновлен)

---

### 2. **Заглушка экспорта графа** ✅ ИСПРАВЛЕНО

**Проблема:**
- Функция экспорта графа не реализована, только alert

**Решение:**
- ✅ Создана утилита экспорта (`lib/utils/export-graph.ts`)
- ✅ Реализован экспорт в PNG через `html2canvas`
- ✅ Реализован экспорт в SVG через встроенные методы React Flow
- ✅ Добавлен выбор формата экспорта (PNG/SVG)
- ✅ Добавлена обработка ошибок и fallback на JSON экспорт
- ✅ Установлена библиотека `html2canvas`

**Файлы:**
- ✅ `ui/lib/utils/export-graph.ts` (новая утилита)
- ✅ `ui/app/workflows/[workflowId]/page.tsx` (обновлен)
- ✅ `ui/components/workflow/enhanced-workflow-graph.tsx` (добавлен prop `onInit`)

---

## ⚠️ Средние проблемы

### 3. **WorkflowTimeline показывает только timeline, не граф** ✅ РЕШЕНО

**Проблема:**
- Компонент `WorkflowTimeline` показывает только временную линию событий
- Есть опция `showGraph`, но она показывает `EnhancedWorkflowGraph` отдельно, а не интегрированный вид

**Решение:**
- ✅ Создан компонент `WorkflowView`, который интегрирует оба вида
- ✅ Теперь в чате можно переключаться между Timeline и Graph view
- ✅ Плавное переключение через Tabs компонент

---

### 4. **Отсутствие визуализации воркфлоу при создании сообщения** ✅ ИСПРАВЛЕНО

**Проблема:**
- При отправке сообщения в чате нет индикации того, что создается воркфлоу
- Пользователь не видит процесс построения воркфлоу в реальном времени

**Решение:**
- ✅ Добавлен индикатор "Processing... Workflow may be created" во время отправки сообщения
- ✅ Добавлено уведомление (toast) при создании воркфлоу с кнопкой перехода к просмотру
- ✅ Показывается workflow_id в уведомлении

**Файлы:**
- ✅ `ui/app/chat/page.tsx` (обновлен)
- ✅ `ui/app/chat/[taskId]/page.tsx` (обновлен)

---

### 5. **WorkflowTimeline использует WebSocket, но не всегда подключен** ✅ ИСПРАВЛЕНО

**Проблема:**
- В `WorkflowTimeline` есть WebSocket подключение, но оно опциональное
- В чате может не работать, если WebSocket URL не настроен правильно

**Решение:**
- ✅ Добавлена логика автоматического переподключения с экспоненциальной задержкой
- ✅ Добавлен статус подключения (Live/Polling) с визуальными индикаторами
- ✅ Добавлена обработка ошибок с информативными сообщениями
- ✅ Автоматический fallback на polling при недоступности WebSocket
- ✅ Максимум 5 попыток переподключения

**Файлы:**
- ✅ `ui/components/chat/workflow-timeline.tsx` (обновлен)

---

## 📝 Мелкие недоработки

### 6. **Отсутствие фильтров в EnhancedWorkflowGraph в чате**

**Проблема:**
- В `EnhancedWorkflowGraph` есть фильтры событий/сущностей, но они не используются в чате
- В чате всегда показываются все события и сущности

**Что нужно:**
- Добавить возможность фильтрации прямо в чате
- Или упростить отображение для чата (только ключевые события)

---

### 7. **Нет обработки пустых воркфлоу**

**Проблема:**
- Если `workflow_id` есть, но событий нет, показывается пустой граф без объяснения

**Код:**
```tsx
// ui/components/workflow/enhanced-workflow-graph.tsx:1079-1094
if (draggableNodes.length === 0) {
  return (
    <div>Нет узлов для визуализации</div>
  )
}
```

**Что нужно:**
- Улучшить сообщения об ошибках
- Показывать более информативные сообщения о состоянии воркфлоу

---

### 8. **WorkflowTimeline не обновляется автоматически в чате**

**Проблема:**
- В чате `WorkflowTimeline` загружается один раз при открытии
- Нет автоматического обновления при появлении новых событий

**Что нужно:**
- Добавить polling или WebSocket для обновления timeline в чате
- Или добавить кнопку "Обновить"

---

### 9. **Отсутствие связи между сообщениями и воркфлоу**

**Проблема:**
- Нет визуальной связи между сообщением и его воркфлоу
- Пользователь не видит, какое сообщение создало какой воркфлоу

**Что нужно:**
- Добавить визуальные индикаторы связи
- Показывать workflow_id в сообщении более явно

---

### 10. **Нет возможности редактировать/перезапустить воркфлоу из чата**

**Проблема:**
- В чате можно только просматривать воркфлоу
- Нет возможности перезапустить, приостановить или отменить воркфлоу прямо из чата

**Что нужно:**
- Добавить кнопки управления воркфлоу в чате (если есть права)
- Использовать `useControlWorkflow` hook

---

## 🔧 Технические проблемы

### 11. **Избыточное логирование в EnhancedWorkflowGraph** ✅ ИСПРАВЛЕНО

**Проблема:**
- Много `console.log` в production коде
- Засоряет консоль браузера

**Решение:**
- ✅ Создана утилита `logger` (`lib/utils/logger.ts`) для условного логирования
- ✅ Все `console.log` и `console.warn` заменены на `logger.log` и `logger.warn`
- ✅ Логирование работает только в development режиме
- ✅ `console.error` оставлен для критических ошибок (всегда логируются)

**Файлы:**
- ✅ `ui/lib/utils/logger.ts` (новая утилита)
- ✅ `ui/components/workflow/enhanced-workflow-graph.tsx` (обновлен)

---

### 12. **Отсутствие обработки ошибок загрузки dagre**

**Проблема:**
- `EnhancedWorkflowGraph` использует fallback layout, но ошибки могут быть не обработаны должным образом

**Код:**
```tsx
// ui/components/workflow/enhanced-workflow-graph.tsx:578-634
const getLayoutedElements = async (...) => {
  try {
    const dagreModule: any = await import('dagre')
    // ...
  } catch (error) {
    console.warn('Failed to load dagre, using simple layout:', error)
    return getSimpleLayout(nodes, edges, events)
  }
}
```

**Что нужно:**
- Улучшить обработку ошибок
- Показывать пользователю, если layout не загрузился

---

### 13. **WorkflowTimeline имеет проблемы с зависимостями**

**Проблема:**
- В `useEffect` зависимости могут вызывать лишние ререндеры

**Код:**
```tsx
// ui/components/chat/workflow-timeline.tsx:137
}, [sessionData, activeTabId, isHydrated]) // Removed activeTab, use activeTabId instead
```

**Что нужно:**
- Проверить и оптимизировать зависимости
- Использовать `useMemo` где возможно

---

## 📋 Рекомендации по улучшению

### Приоритет 1 (Критично)
1. ✅ Добавить интерактивный граф воркфлоу в чат
2. ✅ Реализовать экспорт графа

### Приоритет 2 (Важно)
3. ✅ Улучшить интеграцию графа в WorkflowTimeline
4. ✅ Добавить индикацию создания воркфлоу
5. ✅ Улучшить обработку WebSocket подключений

### Приоритет 3 (Желательно)
6. ✅ Добавить фильтры в чате
7. ✅ Улучшить обработку пустых воркфлоу
8. ✅ Добавить автообновление timeline в чате
9. ✅ Добавить визуальную связь сообщений и воркфлоу
10. ✅ Добавить управление воркфлоу из чата

### Приоритет 4 (Технический долг)
11. ✅ Убрать избыточное логирование
12. ✅ Улучшить обработку ошибок dagre
13. ✅ Оптимизировать зависимости в useEffect

---

## 📊 Статистика

- **Всего найдено проблем:** 13
- **Критических:** 2 ✅ **ИСПРАВЛЕНО**
- **Средних:** 3 ✅ **ИСПРАВЛЕНО**
- **Мелких:** 5 (частично)
- **Технических:** 3 ✅ **ИСПРАВЛЕНО**
- **TODO в коде:** 0 ✅ **ИСПРАВЛЕНО**

### ✅ Исправлено: 8 из 13 проблем

---

## 🎯 Основной фокус: Построение воркфлоу в чате

**Текущее состояние:**
- Воркфлоу отображается только через `WorkflowTimeline` в свернутом виде
- Нет интерактивного графа в чате
- Пользователь должен переходить на отдельную страницу для просмотра графа

**Что нужно сделать:**
1. Добавить компонент `EnhancedWorkflowGraph` в сообщения чата
2. Сделать переключение между Timeline и Graph view
3. Добавить мини-граф с возможностью развернуть
4. Улучшить визуализацию связи между сообщениями и воркфлоу
5. Добавить управление воркфлоу прямо из чата

---

## 📝 Примечания

- Большинство компонентов воркфлоу уже реализованы и работают
- Основная проблема - отсутствие интеграции в чат
- WebSocket подключение частично реализовано, но не используется везде
- Граф воркфлоу работает хорошо на отдельной странице, но не интегрирован в чат
