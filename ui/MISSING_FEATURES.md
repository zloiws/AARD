# 🚨 Отсутствующий функционал из старого UI

## Критически важные функции

### 1. Управление серверами Ollama ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Список серверов (`/servers`)
- Создание сервера
- Редактирование сервера
- Удаление сервера
- Проверка доступности сервера
- Настройка приоритета и capabilities
- Установка default сервера

**API endpoints:**
- `GET /api/servers` - список серверов
- `POST /api/servers` - создание сервера
- `GET /api/servers/{id}` - детали сервера
- `PUT /api/servers/{id}` - обновление сервера
- `DELETE /api/servers/{id}` - удаление сервера
- `POST /api/servers/{id}/discover` - обнаружение моделей

**Файлы для создания:**
- `app/servers/page.tsx` - список серверов
- `app/servers/new/page.tsx` - создание сервера
- `app/servers/[id]/page.tsx` - детали сервера
- `app/servers/[id]/edit/page.tsx` - редактирование

---

### 2. Управление моделями ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Список моделей по серверу
- Настройка capabilities (code_generation, reasoning, general_chat, etc.)
- Настройка priority
- Включение/отключение моделей (is_active)
- Проверка доступности модели
- Синхронизация моделей с сервером

**API endpoints:**
- `GET /api/servers/{id}/models` - модели сервера
- `GET /api/models/{id}` - детали модели
- `PUT /api/models/{id}` - обновление модели (capabilities, priority, is_active)
- `POST /api/models/{id}/check-availability` - проверка доступности
- `POST /api/models/server/{id}/check-all` - проверка всех моделей

**Файлы для создания:**
- `app/servers/[id]/models/page.tsx` - список моделей сервера
- `app/models/[id]/page.tsx` - детали модели
- `app/models/[id]/edit/page.tsx` - редактирование модели

---

### 3. Настройка выбора моделей для типов задач ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Настройка mapping: task_type -> model
- Настройка приоритетов моделей для каждого типа задачи
- Возможность указать конкретную модель для конкретного типа задачи
- Fallback модели

**Текущая проблема:**
- Система выбирает первую модель наугад
- Нет настройки, какая модель для какого типа задачи
- Нет возможности указать приоритеты

**Решение:**
- Добавить настройки в Settings или отдельную страницу
- Использовать capabilities и priority моделей
- Создать mapping task_type -> model selection rules

---

### 4. Тест моделей (Benchmarks) ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Выбор benchmark задач
- Выбор моделей для тестирования
- Запуск тестов
- Просмотр результатов
- Сравнение моделей
- Статистика по тестам

**API endpoints:**
- `GET /api/benchmarks/tasks/` - список benchmark задач
- `POST /api/benchmarks/run/` - запуск теста
- `GET /api/benchmarks/results/` - результаты тестов
- `GET /api/benchmarks/comparison/` - сравнение моделей
- `GET /api/benchmarks/stats/` - статистика

**Файлы для создания:**
- `app/benchmarks/page.tsx` - выбор задач и моделей
- `app/benchmarks/results/page.tsx` - результаты тестов
- `app/benchmarks/comparison/page.tsx` - сравнение моделей

---

### 5. Agent Gym (Тесты агентов) ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Список тестов агентов
- Создание теста
- Запуск теста
- Просмотр результатов
- Статистика по тестам

**API endpoints:**
- `GET /api/agent-gym/tests` - список тестов
- `POST /api/agent-gym/tests` - создание теста
- `POST /api/agent-gym/tests/{id}/run` - запуск теста
- `GET /api/agent-gym/tests/{id}/runs` - результаты запусков

**Файлы для создания:**
- `app/agent-gym/page.tsx` - список тестов
- `app/agent-gym/new/page.tsx` - создание теста
- `app/agent-gym/[id]/page.tsx` - детали теста

---

## Дополнительные функции

### 6. Traces (Трассировка) ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Список трасс
- Детали трассы
- Просмотр spans

**API endpoints:**
- `GET /api/traces/` - список трасс
- `GET /api/traces/{id}` - детали трассы
- `GET /api/traces/{id}/spans` - spans трассы

---

### 7. Artifacts (Артефакты) ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Список артефактов
- Создание артефакта
- Детали артефакта

**API endpoints:**
- `GET /api/artifacts/` - список артефактов
- `POST /api/artifacts/` - создание артефакта
- `GET /api/artifacts/{id}` - детали артефакта

---

### 8. Approvals (Одобрения) ⚠️
**Статус:** Отсутствует в новом UI

**Что нужно:**
- Очередь одобрений
- Детали запроса на одобрение
- Одобрение/отклонение

**API endpoints:**
- `GET /api/approvals/` - список запросов
- `GET /api/approvals/{id}` - детали запроса
- `POST /api/approvals/{id}/approve` - одобрить
- `POST /api/approvals/{id}/reject` - отклонить

---

### 9. Settings (Расширенные настройки) ⚠️
**Статус:** Частично реализовано

**Что есть:**
- Базовые настройки API

**Что отсутствует:**
- Feature Flags управление
- Logging Levels управление
- Module Settings
- System Settings (из базы данных)

**API endpoints:**
- `GET /api/settings/` - все настройки
- `POST /api/settings/` - обновление настройки
- `GET /api/settings/features/all` - feature flags
- `POST /api/settings/features/` - обновление feature flag
- `GET /api/settings/logging/all` - logging levels
- `POST /api/settings/logging/` - обновление logging level

---

## Приоритеты

### Высокий приоритет (критично)
1. ✅ **Управление серверами** - реализовано
2. ✅ **Управление моделями** - реализовано
3. ✅ **Настройка выбора моделей** - реализовано (использует capabilities и priority)
4. ✅ **Тест моделей** - реализовано (benchmarks UI)

### Средний приоритет
5. Agent Gym
6. Расширенные Settings
7. Traces
8. Artifacts
9. Approvals

---

## Текущая проблема с чатом

**Проблема:** Система выбирает первую модель наугад, может выбрать embedding модель

**Решение:**
1. Добавить управление моделями с настройкой capabilities
2. Добавить настройку выбора моделей для типов задач
3. Использовать capabilities и priority при выборе модели

---

## План действий

1. Создать страницы управления серверами
2. Создать страницы управления моделями
3. Добавить настройку выбора моделей
4. Создать UI для тестирования моделей
5. Перенести остальной функционал
