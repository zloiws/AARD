# Руководство по запуску сервера и тестированию

## ✅ Что сделано

1. **Миграция БД применена** - все таблицы созданы
2. **Код исправлен** - все ошибки исправлены
3. **API endpoints созданы** - готовы к тестированию

## 🚀 Запуск сервера

### Вариант 1: Из директории backend (рекомендуется)
```bash
cd C:\work\AARD\backend
python main.py
```

### Вариант 2: Используя скрипты запуска
**Windows (PowerShell):**
```powershell
.\start_server.ps1
```

**Windows (CMD):**
```cmd
start_server.bat
```

### Вариант 3: Через uvicorn из корня (требует настройки PYTHONPATH)
```bash
cd C:\work\AARD
$env:PYTHONPATH="C:\work\AARD\backend"
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

**Важно:** Убедитесь, что `.env` файл существует в `C:\work\AARD\.env`

## 🧪 Тестирование после запуска

### 1. Проверка здоровья сервера
```bash
curl http://localhost:8000/health
```
Ожидаемый ответ: `{"status": "healthy"}`

### 2. Тест создания промпта
```bash
curl -X POST http://localhost:8000/api/prompts/ ^
  -H "Content-Type: application/json" ^
  -d "{\"name\": \"test_prompt\", \"prompt_text\": \"You are helpful\", \"prompt_type\": \"system\", \"level\": 1}"
```

**Ожидаемый результат:** Промпт создан со статусом 200

### 3. Тест создания артефакта
```bash
curl -X POST http://localhost:8000/api/artifacts/ ^
  -H "Content-Type: application/json" ^
  -d "{\"description\": \"Create a tool to add two numbers\", \"artifact_type\": \"tool\"}"
```

**Примечание:** Это займет 30-60 секунд, так как использует LLM для генерации кода.

### 4. Проверка очереди утверждений
```bash
curl http://localhost:8000/api/approvals/
```

После создания артефакта должен появиться запрос на утверждение.

## 📋 Исправления в коде

### 1. Промпты - lowercase значения
- `prompt_type` теперь использует String вместо Enum
- `status` принудительно устанавливается в "active" (lowercase)
- Значения конвертируются в lowercase перед сохранением

### 2. Артефакты - UUID
- Исправлена ошибка с `UUID()` → `uuid4()`

### 3. Database - lazy initialization
- Engine и SessionLocal инициализируются лениво
- Поддержка обратной совместимости через `__getattr__`

### 4. Конфигурация
- `.env` загружается с `override=True`
- Путь к `.env` вычисляется правильно

## ⚠️ Если сервер не запускается

1. **Проверьте .env файл:**
   ```bash
   python backend/fix_env.py
   ```

2. **Проверьте импорты:**
   ```bash
   python backend/test_startup.py
   ```

3. **Проверьте подключение к БД:**
   ```bash
   python -c "from app.core.database import get_engine; engine = get_engine(); print('✅ DB connected')"
   ```

## 🎯 Следующие шаги после успешного запуска

1. ✅ Протестировать создание промпта
2. ✅ Протестировать создание артефакта
3. ✅ Протестировать утверждение артефакта
4. ⏳ Создать веб-интерфейс для утверждений

## 📝 Логи и отладка

Если есть ошибки, проверьте:
- Логи сервера в консоли
- Статус БД подключения
- Доступность Ollama серверов (для генерации артефактов)

