# 🔧 CORS Fix - Инструкция

## ✅ Что было исправлено

1. **CORS middleware перемещен в начало** - теперь обрабатывается первым (важно для preflight запросов)
2. **Добавлен fallback** - `localhost:3000` автоматически добавляется если его нет в настройках
3. **Добавлено логирование** - при запуске backend покажет какие origins разрешены

## 🚀 Что нужно сделать

### 1. Перезапустить Backend

**ОБЯЗАТЕЛЬНО!** Backend нужно перезапустить:

```bash
# Остановите backend (Ctrl+C)
# Затем запустите снова:
cd c:\work\AARD\backend
python main.py
```

При запуске вы должны увидеть в логах:
```
CORS allowed origins: ['http://localhost:8000', 'http://localhost:3000', ...]
```

### 2. Проверить .env файл (опционально)

Убедитесь что в `.env` есть:
```
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000
```

Если нет - добавьте:
```bash
cd c:\work\AARD
echo ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000 >> .env
```

**Но это не обязательно** - код теперь автоматически добавляет localhost:3000!

### 3. Обновить Frontend

Обновите страницу в браузере (F5).

## ✅ Проверка

После перезапуска backend:

1. ✅ В логах backend должно быть: `CORS allowed origins: [...]`
2. ✅ В консоли браузера НЕ должно быть ошибок `OPTIONS 400`
3. ✅ Dashboard должен загружать данные

## 🐛 Если все еще не работает

### Проверьте логи backend

При запуске должно быть:
```
INFO: CORS allowed origins: ['http://localhost:8000', 'http://localhost:3000', ...]
```

Если localhost:3000 нет в списке - значит настройка не применилась.

### Проверьте что backend действительно перезапущен

Остановите полностью (Ctrl+C) и запустите заново.

### Проверьте порты

- Backend: http://localhost:8000
- Frontend: http://localhost:3000

### Проверьте в DevTools

Откройте Network tab:
- OPTIONS запросы должны возвращать `200 OK` (не 400)
- Headers должны содержать:
  ```
  Access-Control-Allow-Origin: http://localhost:3000
  Access-Control-Allow-Methods: *
  Access-Control-Allow-Headers: *
  ```

---

**После перезапуска backend все должно работать!** 🎉
