# Руководство по веб-интерфейсу системы эволюции

## 🚀 Быстрый старт

### 1. Запустите сервер
```powershell
cd C:\work\AARD\backend
python main.py
```

### 2. Откройте в браузере

**Очередь утверждений:**
```
http://localhost:8000/approvals
```

**Список артефактов:**
```
http://localhost:8000/artifacts
```

**Создание артефакта:**
```
http://localhost:8000/artifacts/create
```

## 📋 Доступные страницы

### Очередь утверждений (`/approvals`)

**Что показывает:**
- Все запросы на утверждение в статусе `pending`
- Тип запроса (новый артефакт, обновление, изменение промпта)
- Уровень риска (высокий/средний/низкий)
- Рекомендации системы
- Время создания и таймаут

**Действия:**
- Клик по карточке → переход к деталям
- Автообновление каждые 30 секунд

### Детали утверждения (`/approvals/{id}`)

**Что показывает:**
- Полная информация о запросе
- Код артефакта (если это инструмент)
- Промпт артефакта (если это агент)
- Оценка безопасности с деталями
- Рекомендации системы

**Действия:**
- **Утвердить** - с комментарием (необязательно)
- **Отклонить** - с указанием причины (обязательно)
- После обработки → автоматический переход к очереди

### Список артефактов (`/artifacts`)

**Что показывает:**
- Все артефакты (агенты и инструменты)
- Статус каждого артефакта
- Версия, безопасность, автор
- Дата создания

**Действия:**
- Фильтрация по типу (tool/agent)
- Фильтрация по статусу (active/waiting_approval/deprecated)
- Клик по карточке → переход к деталям
- Кнопка "Создать артефакт" → форма создания

### Создание артефакта (`/artifacts/create`)

**Что нужно заполнить:**
1. **Тип артефакта** (обязательно)
   - Инструмент (Tool) - выполняет конкретные задачи
   - Агент (Agent) - использует инструменты для решения задач

2. **Описание** (обязательно)
   - Подробное описание того, что должен делать артефакт
   - Чем подробнее, тем лучше результат

3. **Дополнительный контекст** (необязательно)
   - JSON объект с дополнительной информацией
   - Например: `{"requirements": ["async"], "similar_tools": ["file_search"]}`

**Процесс:**
1. Заполните форму
2. Нажмите "Создать артефакт"
3. Дождитесь генерации (30-60 секунд)
4. Автоматический переход к деталям созданного артефакта

### Детали артефакта (`/artifacts/{id}`)

**Что показывает:**
- Полная информация об артефакте
- Код (для инструментов)
- Промпт (для агентов)
- Результаты тестирования
- Оценка безопасности
- Метаданные (версия, статус, автор)

**Особенности:**
- Если артефакт в статусе `waiting_approval`, есть ссылка на очередь утверждений
- Подсветка кода для лучшей читаемости

## 🔄 Типичный workflow

### Создание и утверждение артефакта:

1. **Создайте артефакт:**
   - Перейдите на `/artifacts/create`
   - Заполните форму
   - Дождитесь генерации

2. **Проверьте результат:**
   - Автоматически откроется страница деталей
   - Просмотрите код/промпт
   - Проверьте оценку безопасности

3. **Утвердите артефакт:**
   - Перейдите на `/approvals`
   - Найдите созданный запрос
   - Откройте детали
   - Нажмите "Утвердить" или "Отклонить"

4. **Артефакт активирован:**
   - После утверждения артефакт получает статус `active`
   - Он доступен для использования

## 💡 Советы

### Для лучших результатов генерации:

1. **Подробное описание:**
   - Укажите конкретные требования
   - Опишите входные и выходные данные
   - Упомяните особенности реализации

2. **Дополнительный контекст:**
   - Используйте JSON для структурированной информации
   - Укажите похожие артефакты для примера
   - Добавьте требования (async, error handling и т.д.)

3. **Проверка безопасности:**
   - Всегда проверяйте оценку безопасности
   - Просматривайте код перед утверждением
   - Обращайте внимание на рекомендации системы

## 🎨 Особенности интерфейса

- **Автообновление** - очередь утверждений обновляется автоматически
- **Цветовая индикация** - типы и статусы выделены цветом
- **Адаптивный дизайн** - работает на всех устройствах
- **Подсветка кода** - код отображается с подсветкой синтаксиса
- **Плавные переходы** - все действия с анимациями

## ✅ Готово к использованию!

Веб-интерфейс полностью функционален и готов к работе. Перезапустите сервер и откройте страницы в браузере!

