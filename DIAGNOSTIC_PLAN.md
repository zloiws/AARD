# План диагностики и исправления проблем с выбором сервера/модели

## 🔍 Выявленные проблемы

### 1. При выборе/смене сервера не подгружается список моделей
**Причина:** 
- Функция `loadServerModels()` может не обрабатывать правильно ответ от API
- Возможно, модели не загружены в БД для выбранного сервера
- Нет логирования для отладки

**Исправления:**
- ✅ Добавлено логирование в `loadServerModels()`
- ✅ Улучшена обработка ошибок
- ✅ Добавлен fallback на `/api/models/servers`
- ✅ Добавлено состояние загрузки

### 2. Даже при смене сервера доступен только "автоматический выбор"
**Причина:**
- Модели не загружаются из БД
- API `/api/servers/{server_id}/models` может возвращать пустой массив
- Модели могут быть не синхронизированы с сервером

**Исправления:**
- ✅ Улучшена обработка пустого ответа
- ✅ Добавлен fallback
- ⏳ Нужно добавить кнопку "Обновить модели" в настройках

### 3. Всегда отвечает только одна модель
**Причина:**
- `handleSubmit()` неправильно отправляет данные через HTMX
- `hx-vals` может не работать с JSON
- API может игнорировать `server_id` и `model`

**Исправления:**
- ✅ Переписан `handleSubmit()` - теперь использует `htmx.ajax()` напрямую
- ✅ Добавлено логирование запроса
- ✅ Улучшена передача `server_id` и `model` в API

## 📋 План дальнейших действий

### Этап 1: Исправление текущих проблем ✅
- [x] Исправить `loadServerModels()` - добавить логирование
- [x] Исправить `handleSubmit()` - правильная отправка данных
- [x] Добавить логирование в API chat.py

### Этап 2: Создание вкладки "Настройки" ⏳
- [ ] Создать страницу настроек
- [ ] Добавить управление серверами (добавление/удаление)
- [ ] Добавить синхронизацию моделей с серверами
- [ ] Добавить проверку доступности моделей
- [ ] Добавить настройку параметров моделей (тип задачи, промпты)

### Этап 3: Привязка параметров моделей к БД ⏳
- [ ] Создать таблицу `model_configs` в БД
- [ ] Добавить поля: task_types, system_prompts, capabilities
- [ ] Создать API для управления конфигурациями моделей
- [ ] Интегрировать в интерфейс настроек

## 🔧 Технические детали

### Исправления в frontend/templates/main.html

1. **loadServerModels():**
   - Добавлено логирование через `console.log()`
   - Улучшена обработка ошибок
   - Добавлено состояние загрузки (disabled)
   - Добавлен fallback на `/api/models/servers`

2. **handleSubmit():**
   - Переписан для использования `htmx.ajax()` напрямую
   - Правильная передача JSON данных
   - Добавлено логирование запроса
   - Очистка поля ввода после отправки

### Исправления в backend/app/api/routes/chat.py

1. **Обработка server_id:**
   - Добавлено логирование выбранного сервера
   - Улучшена обработка случая, когда server_url есть, но model нет

## 🚀 Следующие шаги

1. Протестировать исправления
2. Создать вкладку "Настройки"
3. Добавить функционал управления серверами
4. Добавить синхронизацию моделей
5. Добавить привязку параметров к БД

