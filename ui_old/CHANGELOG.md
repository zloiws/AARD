# Changelog - Frontend Improvements

## [2024-12-XX] - Workflow Visualization Improvements

### ✅ Добавлено

#### Интерактивный граф воркфлоу в чате
- Новый компонент `WorkflowView` для отображения воркфлоу с переключением между Timeline и Graph view
- Интеграция `EnhancedWorkflowGraph` прямо в сообщения чата
- Компактный режим для чата с возможностью развернуть граф
- Переключение между Timeline и Graph view без перехода на другую страницу

**Файлы:**
- `components/chat/workflow-view.tsx` - новый компонент
- `app/chat/page.tsx` - обновлен для использования WorkflowView
- `app/chat/[taskId]/page.tsx` - обновлен для использования WorkflowView

#### Экспорт графа воркфлоу
- Экспорт графа в PNG формат (через html2canvas)
- Экспорт графа в SVG формат (нативный)
- Выбор формата экспорта (PNG/SVG)
- Обработка ошибок с fallback на JSON экспорт

**Файлы:**
- `lib/utils/export-graph.ts` - утилиты экспорта
- `app/workflows/[workflowId]/page.tsx` - добавлена функциональность экспорта
- `components/workflow/enhanced-workflow-graph.tsx` - добавлен prop `onInit` для доступа к React Flow instance

### 📦 Зависимости

- Добавлена библиотека `html2canvas` для экспорта PNG

### 🔧 Технические детали

#### WorkflowView Component
```tsx
<WorkflowView 
  workflowId={workflowId} 
  compact={true}  // Компактный режим для чата
/>
```

Компонент автоматически:
- Загружает события и сущности воркфлоу
- Показывает индикатор загрузки
- Предоставляет переключение между Timeline и Graph view
- В компактном режиме показывает граф в свернутом `<details>`

#### Export Functionality
```tsx
// PNG Export
await exportGraphToPNG(reactFlowInstance, containerElement, {
  format: 'png',
  filename: 'workflow.png',
  backgroundColor: '#ffffff',
  scale: 2
})

// SVG Export
await exportGraphToSVG(reactFlowInstance, containerElement, {
  format: 'svg',
  filename: 'workflow.svg',
  backgroundColor: '#ffffff'
})
```

### 🐛 Исправлено

- Удалены неиспользуемые импорты `Clock` из файлов чата
- Улучшена интеграция графа воркфлоу в чат

### ✅ Дополнительные улучшения

#### Улучшенная обработка WebSocket подключений
- Автоматическое переподключение с экспоненциальной задержкой (до 5 попыток)
- Визуальные индикаторы статуса подключения (Live/Polling)
- Обработка ошибок с информативными сообщениями
- Автоматический fallback на polling при недоступности WebSocket

**Файлы:**
- `components/chat/workflow-timeline.tsx` - улучшена логика WebSocket

#### Индикация создания воркфлоу
- Индикатор "Processing... Workflow may be created" во время отправки сообщения
- Toast-уведомление при создании воркфлоу с кнопкой перехода к просмотру
- Отображение workflow_id в уведомлении

**Файлы:**
- `app/chat/page.tsx` - добавлена индикация
- `app/chat/[taskId]/page.tsx` - добавлена индикация

#### Оптимизация логирования
- Создана утилита `logger` для условного логирования (только в dev режиме)
- Все debug-логи в `EnhancedWorkflowGraph` теперь работают только в development
- Критические ошибки логируются всегда

**Файлы:**
- `lib/utils/logger.ts` - новая утилита
- `components/workflow/enhanced-workflow-graph.tsx` - заменены все console.log на logger

### 📝 Следующие шаги

- [ ] Добавить фильтры событий/сущностей в чате
- [ ] Улучшить обработку пустых воркфлоу
- [ ] Добавить автообновление timeline в чате
- [ ] Добавить визуальную связь сообщений и воркфлоу
- [ ] Добавить управление воркфлоу из чата
