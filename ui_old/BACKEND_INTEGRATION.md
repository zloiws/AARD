# Backend Integration Guide

## ✅ Что было сделано

### 1. CORS Configuration
- ✅ Добавлен `ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000` в `.env`
- ✅ Backend теперь разрешает запросы с `localhost:3000`

### 2. API Client Updated
- ✅ Обновлены endpoints для работы с реальным backend:
  - Tasks: `/api/current-work/tasks` (вместо `/api/tasks`)
  - Plans: `/api/plans` ✅
  - Agents: `/api/agents` ✅
  - Chat: `/api/chat` ✅

### 3. Data Types Updated
- ✅ Типы данных адаптированы под реальные структуры ответов backend
- ✅ Компоненты dashboard обновлены для работы с новой структурой

## 🚀 Что нужно сделать

### 1. Перезапустить Backend

**Важно!** Backend нужно перезапустить чтобы подхватить новую настройку CORS:

```bash
# Остановите текущий backend (Ctrl+C)
# Затем запустите снова:
cd c:\work\AARD\backend
python main.py
```

### 2. Проверить .env файл

Убедитесь что в `.env` есть:
```
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000
```

Если нет - добавьте вручную или выполните:
```bash
cd c:\work\AARD
echo ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000 >> .env
```

### 3. Обновить Frontend

Обновите страницу в браузере (F5 или Ctrl+R).

## 🧪 Проверка

После перезапуска backend:

1. **Frontend должен загружаться без CORS ошибок**
2. **В консоли браузера не должно быть ошибок OPTIONS 400**
3. **Dashboard должен показывать данные** (если есть активные задачи)

## 📊 API Endpoints Mapping

| Frontend API | Backend Endpoint | Status |
|--------------|------------------|--------|
| `api.tasks.list()` | `GET /api/current-work/tasks` | ✅ |
| `api.tasks.get(id)` | `GET /api/current-work/task/{id}` | ✅ |
| `api.plans.list()` | `GET /api/plans` | ✅ |
| `api.plans.get(id)` | `GET /api/plans/{id}` | ✅ |
| `api.agents.list()` | `GET /api/agents` | ✅ |
| `api.agents.get(id)` | `GET /api/agents/{id}` | ✅ |
| `api.chat.sendMessage()` | `POST /api/chat` | ✅ |

## 🔧 Troubleshooting

### CORS ошибки все еще есть?

1. Проверьте что backend перезапущен
2. Проверьте `.env` файл содержит `ALLOWED_ORIGINS`
3. Проверьте что backend запущен на порту 8000
4. Проверьте что frontend на порту 3000

### API возвращает 404?

- Убедитесь что backend запущен
- Проверьте что endpoints существуют в backend
- Проверьте URL в `.env.local` frontend

### Данные не загружаются?

- Откройте DevTools → Network
- Проверьте запросы к API
- Проверьте ответы сервера
- Проверьте что backend возвращает данные

## 📝 Next Steps

После успешного подключения можно:

1. ✅ Создать страницы для детального просмотра задач
2. ✅ Добавить создание новых задач
3. ✅ Реализовать workflow builder с реальными данными
4. ✅ Добавить фильтры и поиск

---

**Готово!** Frontend теперь полностью интегрирован с backend! 🎉
