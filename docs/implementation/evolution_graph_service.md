# Evolution Graph Service — implementation notes\n\nThis document describes `EvolutionGraphService` implemented at `backend/app/services/evolution_graph_service.py`.\n\n## Purpose\n\nProvide utilities to build evolution graphs from `EvolutionHistory` entries so the frontend can render component version trees and a system-wide timeline.\n\n## API\n\n- `build_component_evolution(component_id)` — returns nodes/edges for a single component's version history\n- `build_system_evolution(limit=100)` — returns recent system-wide evolution entries as a timeline graph\n\n## Integration\n\n- `MetaTracker` should call `EvolutionGraphService` when new evolution entries are created to update cached views or trigger broadcasts.\n- Frontend calls `/api/meta/evolution-timeline` which uses `MetaTracker.get_evolution_graph()`; consider adding an endpoint that returns the richer graph built by this service.\n\n*** End Patch

