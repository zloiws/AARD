# Реализация новых функций

## Статус реализации

### ✅ 1. Динамическое получение списка моделей через API

**Реализовано:**
- `GET /api/models/servers` - список всех серверов с их моделями
- `GET /api/models/server/models?server_url=...` - модели конкретного сервера
- Frontend загружает серверы и модели автоматически
- Выбор сервера и модели в интерфейсе

**Файлы:**
- `backend/app/api/routes/models.py` - API endpoints
- `frontend/templates/index_new.html` - новый интерфейс с выбором

### 🔄 2. Кнопка остановки генерации

**Статус:** Частично реализовано

**Реализовано:**
- Кнопка "Остановить" в UI
- Структура для отмены задач (`_active_tasks`)
- Endpoint `/api/chat/cancel/{task_id}`

**Требует доработки:**
- Интеграция с streaming генерацией
- Отслеживание активных задач по session_id
- Правильная отмена async генерации

### 🔄 3. Чат между моделями

**Статус:** Базовая структура готова

**Реализовано:**
- `POST /api/chat/multi-model` - endpoint для мульти-модельного чата
- Поддержка системных промптов для каждой модели
- Поочередная генерация ответов между моделями

**Требует доработки:**
- UI для настройки системных промптов
- Визуализация разговора между моделями
- Улучшенная логика переключения между моделями

### 🔄 4. Сохранение истории чата

**Статус:** Базовая структура готова (in-memory)

**Реализовано:**
- `ChatSessionManager` - менеджер сессий
- Сохранение сообщений в сессию
- `GET /api/chat/session/{session_id}` - получение истории
- `POST /api/chat/session` - создание сессии

**Требует доработки:**
- Сохранение в БД (сейчас только в памяти)
- Восстановление сессии при обновлении страницы
- LocalStorage для временного хранения session_id
- UI для управления сессиями

### 🔄 5. Вкладка метрики

**Статус:** Заглушка создана

**Требует реализации:**
- Сбор метрик использования моделей
- Статистика по запросам
- Визуализация метрик
- Сохранение метрик в БД

## Примечание о GPU

Модели в Ollama загружаются в GPU **по требованию**:
- При первом запросе модель загружается в GPU
- Модель остается в GPU памяти, пока не выгрузится (через timeout или вытеснение)
- Статус загрузки модели можно проверить через `/api/ps` endpoint

**Проверка загруженных моделей:**
```python
# В Ollama API
GET /api/ps  # Показывает загруженные в GPU модели
```

## Следующие шаги

1. **Исправить остановку генерации:**
   - Интегрировать с streaming
   - Реализовать правильную отмену async задач

2. **Добавить сохранение сессий в БД:**
   - Создать таблицу `chat_sessions`
   - Миграция Alembic
   - Восстановление сессий

3. **Завершить UI для мульти-модельного чата:**
   - Форма для системных промптов
   - Визуализация разговора

4. **Реализовать метрики:**
   - Модель для метрик
   - Сбор данных
   - Визуализация

