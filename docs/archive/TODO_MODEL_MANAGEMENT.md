# 📋 TODO: Управление моделями и GPU

## Приоритет: Высокий

### 1. ✅ Правильное чтение состояния модели (загружена или нет в GPU)

**Статус:** Частично реализовано (заглушка)

**Текущее состояние:**
- Есть endpoint `/api/models/{model_id}/unload` который проверяет `/api/ps`
- Проверка работает, но нужно улучшить обработку

**Задачи:**
- [ ] Улучшить обработку ответа от `/api/ps` Ollama API
- [ ] Добавить кэширование состояния модели
- [ ] Добавить периодическую проверку состояния загруженных моделей
- [ ] Отображать статус модели в UI (загружена/не загружена)

**Файлы для доработки:**
- `backend/app/api/routes/models_management.py` - функция `unload_model()`
- `frontend/templates/settings/index.html` - отображение статуса

---

### 2. ✅ Правильная выгрузка модели из GPU

**Статус:** Не реализовано (заглушка)

**Проблема:**
- Ollama API не имеет прямого endpoint для выгрузки модели из GPU
- Модель выгружается автоматически через таймаут

**Задачи:**
- [ ] Исследовать возможности Ollama API для принудительной выгрузки
- [ ] Реализовать workaround (если доступен):
  - Использовать специальные флаги в API
  - Или другие методы (если есть)
- [ ] Если прямого метода нет - реализовать ожидание таймаута
- [ ] Добавить логирование операций выгрузки

**Файлы для доработки:**
- `backend/app/api/routes/models_management.py` - функция `unload_model()`
- `backend/app/core/ollama_client.py` - добавить метод выгрузки

**Исследование:**
- Проверить документацию Ollama API на наличие скрытых endpoints
- Проверить возможность через `/api/generate` с особыми параметрами
- Проверить возможность через управление процессами Ollama

---

### 3. ✅ Динамическая замена модели при переключении

**Статус:** Не реализовано (заглушка)

**Цель:**
При запросе к модели B, если модель A загружена в GPU:
1. Проверить, какая модель загружена
2. Если другая - выгрузить текущую
3. Загрузить запрошенную модель
4. Выполнить генерацию

**Задачи:**
- [ ] Интегрировать проверку загруженной модели в `OllamaClient.generate()`
- [ ] Добавить логику выгрузки перед загрузкой новой модели
- [ ] Обработать ошибки (если выгрузка не удалась)
- [ ] Добавить настройку: автоматическая замена или ручная
- [ ] Добавить логирование всех операций

**Файлы для доработки:**
- `backend/app/core/ollama_client.py` - метод `generate()`
- `backend/app/api/routes/models_management.py` - функция `unload_model_from_gpu()`
- `backend/app/core/config.py` - добавить настройку `auto_unload_models`

**Алгоритм:**
```python
async def generate(...):
    # 1. Проверить текущую загруженную модель
    loaded_model = await check_loaded_model(server_url)
    
    # 2. Если запрашивается другая модель
    if loaded_model and loaded_model != requested_model:
        # 3. Выгрузить текущую модель
        await unload_model_from_gpu(server_url, loaded_model)
        
        # 4. Подождать выгрузки (если нужно)
        await asyncio.sleep(1)  # или другой механизм
    
    # 5. Выполнить генерацию (модель загрузится автоматически)
    return await _do_generate(...)
```

---

## Дополнительные улучшения

### 4. Мониторинг использования GPU

- [ ] Добавить endpoint для получения списка загруженных моделей
- [ ] Отображать использование GPU памяти
- [ ] Показывать время загрузки модели
- [ ] Предупреждать о нехватке памяти

### 5. Управление приоритетами моделей

- [ ] Автоматическая выгрузка моделей с низким приоритетом
- [ ] Сохранение "горячих" моделей в GPU
- [ ] Настройка таймаутов для разных моделей

---

## Примечания

**Ограничения Ollama API:**
- Нет прямого endpoint для выгрузки модели
- Модели выгружаются автоматически через таймаут
- `/api/ps` показывает текущие процессы, но не позволяет управлять ими

**Возможные решения:**
1. Использовать таймауты Ollama (настроить через переменные окружения)
2. Перезапуск Ollama сервиса (не рекомендуется)
3. Ожидание естественной выгрузки (не идеально)
4. Запросить функцию в Ollama (долгосрочно)

**Приоритет реализации:**
1. Правильное чтение состояния (важно для понимания ситуации)
2. Динамическая замена (важно для удобства)
3. Правильная выгрузка (зависит от возможностей API)

