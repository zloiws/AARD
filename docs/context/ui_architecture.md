# AARD Web UI — Functional Layout & UX Spec (for Cursor)

## 0. Общий принцип UI (фиксируется сразу)

UI в AARD — это **операционная панель среды**, а не чат-бот.

Основные качества:

* информативность > визуал
* причинно-следственные связи > анимации
* одинаково удобно для:

  * наблюдения
  * анализа
  * принятия решений человеком

**Цвета / шрифты вторичны.**

---

## 1. Общий Layout (1 экран, 3 зоны)

```
+-----------------------------------------------------+
| Top Bar                                             |
+----------------------+------------------------------+
| Left Sidebar         | Main Workspace               |
|                      |                              |
|                      |                              |
+----------------------+------------------------------+
| Bottom Status / Logs                                |
+-----------------------------------------------------+
```

---

## 2. Top Bar (глобальный контроль)

### Назначение

Контекст всей среды.

### Содержит

* Название среды: `AARD`
* Active Session ID / Workflow ID
* Статус backend:

  * 🟢 connected
  * 🟡 degraded
  * 🔴 offline
* Кнопки:

  * `Settings`
  * `Registry`
  * `Prompts`
  * `Pause / Resume execution`

**Без логики. Только навигация и статус.**

---

## 3. Left Sidebar (режимы работы)

Вертикальный список режимов:

```
[ Chat ]
[ Timeline ]
[ Graph ]
[ Registry ]
[ Prompts ]
[ Settings ]
```

Это **не роутинг страниц**, а **переключение представления** одного состояния.

---

## 4. Main Workspace — ключевые режимы

### 4.1 Chat View (Interaction Layer)

**Назначение:** ввод и получение сообщений.

#### Структура

```
---------------------------------
| Message History               |
|                               |
---------------------------------
| Input Box                     |
| [ Send ]                      |
---------------------------------
```

#### Особенности

* сообщения помечены:

  * User
  * System (clarification)
  * Result
* если вопрос от Validator A:

  * визуально помечен как `Clarification Required`
* **нет указания, кто ответил** (агент/LLM/инструмент)

Чат — *чёрный ящик*, всё объяснение — в Timeline / Graph.

---

### 4.2 Timeline View (Observability №1)

**Это главный экран системы.**

#### Вертикальная шкала событий

Каждый блок = `ExecutionEvent`.

```
[ Interpretation ]
  - component
  - prompt_id@version
  - input_summary
  - output_summary
  - reason_code

[ Validator A ]
[ Routing ]
[ Planning ]
[ Validator B ]
[ Execution ]
[ Reflection ]
```

#### Поведение

* блоки кликабельны
* раскрываются детали
* видно:

  * **почему был сделан шаг**
  * **что повлияло на решение**
* если был создан агент:

  * отдельный event с ссылкой на Registry

Если Timeline не даёт ответа «почему» — это баг backend’а.

---

### 4.3 Graph View (Observability №2)

**Граф = визуальный след мышления.**

#### Типы нод

* User
* Component
* Agent
* Tool
* Registry

#### Типы рёбер

* interpreted_as
* validated_by
* planned_by
* executed_by
* created_by
* queried

#### Поведение

* строится автоматически из ExecutionEvent
* клик по ноде:

  * подсветка связанных событий в Timeline
* клик по ребру:

  * причина (`reason_code`)
  * timestamp

**Graph не редактируется вручную. Никогда.**

---

## 5. Registry View

**Read-only по умолчанию.**

### Tabs

```
[ Agents ] [ Tools ] [ Capabilities History ]
```

#### Для агента видно:

* agent_id
* роль
* system_prompt_version
* confidence
* lifecycle
* кто и когда создал

#### Действия

* `Propose Update`
* `Deprecate`
  (всегда через backend, с подтверждением)

---

## 6. Prompts View

### Назначение

Контроль runtime-промптов.

#### Отображает

* список system prompts
* версии
* где используется (component / agent)
* assignments:

  * server
  * model
  * task_type

#### Действия

* создать новую версию
* назначить
* откатить

**Inline-редактирование запрещено.**

---

## 7. Settings View

### Разделы

#### 7.1 Servers

* список серверов
* тип (local / remote)
* статус
* API endpoint

#### 7.2 Models

* модель
* роль (reasoning / code)
* enabled / disabled

#### 7.3 API Keys

* сторонние сервисы
* masked view
* scope

Любое изменение:

* подтверждение
* логируется
* отображается в Timeline как system event

---

## 8. Bottom Status / Logs

Минимальная строка:

* текущая стадия
* активный компонент
* ошибки execution

Не дублирует Timeline.

---

## 9. UX-правила (очень важно для Cursor)

Передай Cursor буквально:

* ❌ никаких “умных” подсказок от UI
* ❌ никаких вычислений причин на фронте
* ❌ никаких скрытых состояний
* ✅ всё приходит из backend
* ✅ UI — чистая проекция состояния среды

---

## 10. Компонентная структура (React)

Примерно так:

```
App
 ├─ TopBar
 ├─ Sidebar
 ├─ Workspace
 │   ├─ ChatView
 │   ├─ TimelineView
 │   ├─ GraphView
 │   ├─ RegistryView
 │   ├─ PromptsView
 │   └─ SettingsView
 └─ StatusBar
```

State:

* global execution state (WS)
* selected session / workflow
* UI mode

---

## 11. Как сказать Cursor’у начать

Рекомендованная формулировка:

> «Собери UI строго по этому описанию.
> Без дизайнерских улучшений.
> Сначала каркас + mock data.
> Потом подключение WebSocket событий.»

---
