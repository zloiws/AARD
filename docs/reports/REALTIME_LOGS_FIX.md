# Исправление real-time отображения логов моделей

## Проблема

Логи моделей должны отображаться в реальном времени во время генерации, но сейчас они появляются только после завершения.

## Решение

### Backend изменения

1. **Real-time сохранение логов** - логи сохраняются в Digital Twin контекст сразу при создании
   - Добавлено поле `current_task_id` в PlanningService
   - В методе `_add_model_log()` логи сохраняются в базу сразу, если task_id известен
   - Логи сохраняются по мере создания, а не только в конце

2. **Создание задачи для логирования** - если task_id не передан, создается задача в начале для real-time логирования

### Frontend изменения

1. **Более частое обновление** - обновление каждую секунду вместо 5 секунд
2. **Автоматическое обновление при открытии вкладки** - логи обновляются сразу при открытии
3. **Улучшенная дедупликация** - избегание дублирования логов при обновлении

## Как это работает

1. При начале генерации плана создается/используется задача
2. `current_task_id` устанавливается в PlanningService
3. Каждый раз при создании лога через `_add_model_log()`:
   - Лог добавляется в список `model_logs`
   - Лог сразу сохраняется в Digital Twin контекст задачи
   - Фронтенд получает новые логи при следующем обновлении (каждую секунду)

4. На фронтенде:
   - При открытии вкладки "Логи моделей" начинается обновление каждую секунду
   - Новые логи добавляются без дублирования
   - Авто-прокрутка к новым логам (если включена)

## Тестирование

Для проверки real-time отображения:

1. Откройте вкладку "🔍 Логи моделей" во фронтенде
2. Запустите тест планирования (например, `python scripts/run_test1.py`)
3. Логи должны появляться в реальном времени по мере генерации

## Изменения в коде

### Backend

- `backend/app/services/planning_service.py`:
  - Добавлено поле `current_task_id`
  - Реализовано real-time сохранение логов в `_add_model_log()`
  - Создание задачи в начале для real-time логирования

### Frontend

- `frontend/templates/main.html`:
  - Обновление логов каждую секунду
  - Улучшенная дедупликация
  - Автоматическое обновление при открытии вкладки
