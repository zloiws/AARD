# Интеграция логов моделей во вкладку "Логи моделей"

## Проблема

При тестах планирования логи моделей не отображались во вкладке "Логи моделей", хотя вкладка была создана.

## Концепция: Логи моделей как HITL наблюдение

Логи моделей - это отображение **полного процесса выполнения операций** для Human-In-The-Loop (HITL) в режиме наблюдения:

1. **Запрос от пользователя** - что именно запросил пользователь
2. **Разбор запроса, определение действий** - как система понимает запрос и что планирует делать
3. **Действия от и до со всеми изменениями** - детальный процесс выполнения с промежуточными состояниями
4. **Результат** - финальный результат операции

Это позволяет человеку наблюдать за работой системы и вмешиваться при необходимости.

## Решение

### 1. Сбор логов в PlanningService

Добавлен сбор логов всех взаимодействий с моделями:
- Запросы к моделям (request)
- Ответы от моделей (response)
- Ошибки (error)

Логи собираются в `PlanningService.model_logs` и автоматически сохраняются в Digital Twin контекст задачи.

### 2. API endpoint для получения логов

Создан новый endpoint `/api/model-logs`:
- `GET /api/model-logs/latest` - получить последние логи из всех задач
- `GET /api/model-logs/task/{task_id}` - получить логи для конкретной задачи

### 3. Автоматическая загрузка на фронтенде

Добавлена функция `loadModelLogsFromAPI()`, которая:
- Автоматически загружает логи при открытии вкладки "Логи моделей"
- Обновляет логи каждые 5 секунд, пока вкладка открыта
- Отображает логи в структурированном виде

## Изменения в коде

### Backend

1. **PlanningService** (`backend/app/services/planning_service.py`):
   - Добавлено поле `model_logs` для сбора логов
   - Добавлен метод `_add_model_log()` для логирования
   - Логи сохраняются в Digital Twin контекст задачи

2. **API Routes** (`backend/app/api/routes/model_logs.py`):
   - Новый роутер для получения логов
   - Endpoints для получения логов из Digital Twin контекста

3. **Main** (`backend/main.py`):
   - Подключен роутер `model_logs`

### Frontend

1. **Main.html** (`frontend/templates/main.html`):
   - Добавлена функция `loadModelLogsFromAPI()`
   - Автоматическая загрузка при открытии вкладки
   - Авто-обновление каждые 5 секунд

## Использование

### Для разработчика

Логи автоматически собираются при вызовах моделей в `PlanningService`:

```python
planning_service = PlanningService(db)
plan = await planning_service.generate_plan(...)

# Логи доступны в planning_service.model_logs
# И сохранены в Digital Twin контекст задачи
```

### Для пользователя

1. Откройте вкладку "🔍 Логи моделей"
2. Логи автоматически загрузятся из последних задач
3. Логи обновляются каждые 5 секунд
4. Используйте фильтры для отображения только нужных типов логов

## Что отображается

### Типы логов (workflow stages)

1. **user_request** - Запрос от пользователя (stage: user_input)
2. **request_analysis** - Разбор запроса, определение действий (stage: analysis)
3. **action_start** - Начало действия (stage: execution)
4. **action_progress** - Прогресс действия со всеми изменениями (stage: execution)
5. **action_end** - Завершение действия (stage: execution)
6. **result** - Финальный результат (stage: completion)
7. **request** - Запросы к моделям (промпты, системные промпты)
8. **response** - Ответы моделей (результаты, JSON)
9. **thinking** - Промежуточные мысли (если поддерживается моделью)
10. **error** - Ошибки выполнения
11. **hallucination_detected** - Обнаружена галлюцинация (TODO)

### Этапы выполнения (stages)

- **user_input** - Получен запрос от пользователя
- **analysis** - Анализ запроса и определение действий
- **execution** - Выполнение действий
- **completion** - Завершение операции

## Структура лога

```json
{
  "type": "request|response|error|thinking",
  "model": "gemma3:4b",
  "content": {...},
  "metadata": {
    "duration_ms": 1234,
    "server": "Server 2 - Coding",
    "task": "analyze_task"
  },
  "timestamp": "2025-12-04T10:00:00"
}
```

## Следующие шаги

1. ✅ Сбор логов в PlanningService
2. ✅ API endpoint для получения логов
3. ✅ Автоматическая загрузка на фронтенде
4. ⏳ Добавить логирование в другие сервисы (ExecutionService, ArtifactService)
5. ⏳ Добавить поддержку WebSocket для real-time обновлений

