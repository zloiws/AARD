# Отображение всех разветвлений (Branching Visualization)

## Обзор

Реализовано полное отображение всех разветвлений системы для понимания:
- Что пошло так или не так
- Почему выбран тот или иной путь
- Почему выбран тот или иной план
- Почему выбрана та или иная реализация

## Структура разветвлений

### 1. Разветвления планов
- Все версии плана (plan.version)
- Альтернативные подходы (plan.alternatives)
- История репланирования (почему изменился план)
- Выбранный план vs альтернативы

### 2. Разветвления агентов
- Какие агенты были доступны
- Почему выбран конкретный агент
- История использования агентов
- Связь агентов с задачами/планами

### 3. Разветвления промптов
- Какие промпты использовались
- Версии промптов (Prompt.version, Prompt.parent_prompt_id)
- Системные промпты
- Промпты агентов/инструментов
- Почему выбран конкретный промпт

### 4. Разветвления инструментов
- Какие инструменты использовались
- История использования инструментов
- Зависимости между инструментами (ArtifactDependency)
- Почему выбран конкретный инструмент

## Digital Twin контекст

Все разветвления сохраняются в Digital Twin контекст задачи (Task.context JSONB):

```json
{
  "planning_decisions": {
    "strategies_considered": [],
    "alternatives": [],
    "replanning_history": []
  },
  "agent_selection": {
    "available_agents": [],
    "selected_agents": [],
    "reasons": {}
  },
  "prompt_usage": {
    "prompts_used": []
  },
  "tool_selection": {
    "available_tools": [],
    "selected_tools": [],
    "reasons": {}
  },
  "memory_storage": {
    "episodic": [],
    "procedural": [],
    "what_stored": "",
    "why_stored": ""
  }
}
```

## Вкладки интерфейса

### 1. "Текущая работа" (Current Work)

**Расположение:** Новая вкладка между "Чат" и "Логи моделей"

**Функциональность:**
- Real-time отображение активных задач (status: planning, in_progress, pending_approval)
- Обновление каждую секунду (автоматически)
- Пошаговое отображение:
  - Поступил запрос
  - Разбираю запрос (с логами)
  - Выполняются шаги (какие, в какой последовательности)
  - Используемые агенты/промпты/инструменты
  - Прогресс до завершения

**Отображение:**
- Список активных задач
- Для каждой задачи - детальное отображение шагов
- Real-time логи моделей (последние 10 событий)
- Прогресс выполнения (progress bar)
- Текущий шаг и его статус

### 2. "Логи моделей" (Model Logs) - обновлено

**Изменения:**
- Убрано авто-обновление
- Добавлена кнопка "🔄 Обновить" для ручной загрузки
- Теперь показывает краткую сводку со всеми разветвлениями

**Функциональность:**
1. Кнопка "🔄 Обновить" - загружает завершенные задачи
2. Краткая сводка с разветвлениями:
   - Запрос пользователя
   - Разбор запроса (анализ + причины)
   - Разветвления планов (все версии, альтернативы, почему выбран)
   - Разветвления агентов (доступные, выбранные, причины)
   - Разветвления промптов (использованные, версии)
   - Разветвления инструментов (использованные, причины)
   - Что записано в долгосрочную память (эпизодическая, процедурная)
   - Что не было сделано и причина

**Отображение:**
- Древовидная структура разветвлений
- Визуализация выбранных путей vs альтернатив
- Причины выбора каждого пути
- Связи между планами, агентами, промптами, инструментами

## API Endpoints

### 1. Краткая сводка с разветвлениями

```
GET /api/model-logs/summary/{task_id}
```

Возвращает структурированную сводку со всеми разветвлениями для конкретной задачи.

**Response:**
```json
{
  "task_id": "uuid",
  "task_description": "...",
  "status": "completed",
  "user_request": "...",
  "request_analysis": {...},
  "plans": [...],
  "plans_alternatives": [...],
  "replanning_history": [...],
  "agents_available": [...],
  "agents_selected": [...],
  "prompts_used": [...],
  "tools_available": [...],
  "tools_used": [...],
  "memory_storage": {...},
  "what_was_done": [...],
  "what_was_not_done": [...],
  "timeline": [...]
}
```

```
GET /api/model-logs/summary/latest?limit=10
```

Возвращает сводки последних завершенных задач.

### 2. Активные задачи (real-time)

```
GET /api/current-work/tasks
```

Возвращает список активных задач с текущим прогрессом.

**Response:**
```json
{
  "tasks": [
    {
      "task_id": "uuid",
      "description": "...",
      "status": "in_progress",
      "current_stage": "executing",
      "progress_percent": 50.0,
      "plan_version": 1,
      "current_step": {...},
      "total_steps": 5,
      "completed_steps": 2,
      "latest_logs": [...],
      "agents_in_use": [...],
      "tools_in_use": [...]
    }
  ],
  "total": 1
}
```

```
GET /api/current-work/task/{task_id}
```

Возвращает детальную информацию о конкретной активной задаче.

## Реализованные изменения

### Backend

1. **Расширены endpoints** (`backend/app/api/routes/model_logs.py`):
   - Добавлен endpoint `/api/model-logs/summary/{task_id}` для сводки с разветвлениями
   - Добавлен endpoint `/api/model-logs/summary/latest` для последних сводок

2. **Создан новый модуль** (`backend/app/api/routes/current_work.py`):
   - Endpoint `/api/current-work/tasks` для активных задач
   - Endpoint `/api/current-work/task/{task_id}` для детальной информации

3. **Расширен Digital Twin контекст** (`backend/app/services/planning_service.py`):
   - Сохранение разветвлений планов (planning_decisions)
   - Сохранение истории репланирования (replanning_history)
   - Структура для агентов, промптов, инструментов
   - Обновление контекста при репланировании

4. **Добавлен роутер** (`backend/main.py`):
   - Подключен роутер `current_work`

### Frontend

1. **Добавлена новая вкладка** (`frontend/templates/main.html`):
   - "Текущая работа" с real-time обновлением
   - Автоматическое обновление каждую секунду

2. **Обновлена вкладка "Логи моделей"**:
   - Кнопка "Обновить" вместо авто-обновления
   - Отображение краткой сводки с разветвлениями
   - Визуализация всех ветвей решений

3. **Добавлены JavaScript функции**:
   - `loadCurrentWork()` - загрузка активных задач
   - `renderActiveTask()` - отображение активной задачи
   - `loadModelLogsSummary()` - загрузка сводки с разветвлениями
   - `renderTaskSummary()` - отображение сводки задачи
   - `escapeHtml()` - экранирование HTML

## Будущие улучшения

1. **Визуализация дерева решений**: Графическое отображение всех разветвлений
2. **Фильтрация разветвлений**: По типам, по времени, по статусу
3. **Экспорт разветвлений**: Сохранение в JSON/PDF для анализа
4. **Сравнение планов**: Side-by-side сравнение разных версий планов
5. **Детекция галлюцинаций**: Автоматическое определение некорректных решений
6. **HITL интеграция**: Уведомления и возможность вмешательства в реальном времени

