# Быстрый старт AARD сервера

## 🚀 Запуск сервера

### Шаг 1: Откройте новый терминал PowerShell

**Важно:** Сервер должен запускаться в отдельном терминале, чтобы вы могли видеть логи.

### Шаг 2: Запустите сервер

```powershell
cd C:\work\AARD\backend
python main.py
```

Вы должны увидеть что-то вроде:
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### Шаг 3: Оставьте этот терминал открытым

**Не закрывайте терминал с сервером!** Сервер должен работать постоянно.

## 🧪 Тестирование

### В другом терминале (новый PowerShell):

```powershell
# Перейдите в корень проекта
cd C:\work\AARD

# Запустите тесты
python backend/test_api_simple.py
```

Или протестируйте вручную:

```powershell
# 1. Проверка здоровья
curl http://localhost:8000/health

# 2. Создание промпта
curl -X POST http://localhost:8000/api/prompts/ -H "Content-Type: application/json" -d '{\"name\": \"test\", \"prompt_text\": \"You are helpful\", \"prompt_type\": \"system\", \"level\": 1}'

# 3. Проверка очереди утверждений
curl http://localhost:8000/api/approvals/
```

## ⚠️ Если сервер не запускается

### Проверьте .env файл:
```powershell
python backend/fix_env.py
```

### Проверьте импорты:
```powershell
python backend/test_startup.py
```

### Проверьте ошибки в терминале сервера
Сервер покажет ошибки при запуске, если что-то не так.

## 🛑 Остановка сервера

В терминале с сервером нажмите `CTRL+C`

## 📝 Примечания

- Сервер должен работать на порту **8000**
- Убедитесь, что порт 8000 не занят другим приложением
- Если порт занят, измените `API_PORT` в `.env` файле

